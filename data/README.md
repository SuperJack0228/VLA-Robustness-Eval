# Demonstration Data

This directory contains large local datasets and is excluded from Git.

- `dataset_v2_clean/`: 1,200 balanced clean expert episodes.
- `dataset_v3_recovery/`: 600 balanced displacement-recovery episodes.

The canonical dataset manifests are preserved in
`artifacts/v3-clean-rc1/dataset_manifests/`.
